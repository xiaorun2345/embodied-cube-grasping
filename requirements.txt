gymnasium>=0.29
imageio[ffmpeg]>=2.34
mujoco>=3.2
numpy>=1.24
tqdm>=4.66

# Heavy training stack. Install with:
#   pip install -e ".[lerobot]"
# or use scripts/setup_lerobot_smolvla.sh to clone LeRobot from source.
