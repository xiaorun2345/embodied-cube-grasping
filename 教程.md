# LeRobot + SmolVLA + MuJoCo 启动教程

本文档用于启动当前工作区已经配置好的 LeRobot 训练框架、SmolVLA 算法环境和 MuJoCo 抓取方块 demo。

项目目录：

```bash
/home/mkls/xiao_run/lerobot_smolvla_mujoco_demo
```

Conda 环境：

```bash
/home/mkls/xiao_run/.conda-lerobot-smolvla
```

核心代码已经按职责拆开：

```text
src/lerobot_smolvla_mujoco_demo/env.py
```

只负责加载 MuJoCo XML、创建 Gymnasium 环境、执行 `reset()` / `step()` / `render()`。

```text
src/lerobot_smolvla_mujoco_demo/scripted_grasp.py
```

只负责脚本专家抓取动作，包括抓方块、移动到托盘、松开夹爪这一套规则动作。

## 1. 进入项目并激活环境

每次新开终端，先执行：

```bash
cd /home/mkls/xiao_run/lerobot_smolvla_mujoco_demo
conda activate /home/mkls/xiao_run/.conda-lerobot-smolvla
```

如果 `conda activate` 不可用，先初始化当前 shell：

```bash
source /home/mkls/anaconda3/etc/profile.d/conda.sh
conda activate /home/mkls/xiao_run/.conda-lerobot-smolvla
```

## 2. 配置 MuJoCo 离屏渲染

服务器或无显示器环境建议使用 EGL：

```bash
export MUJOCO_GL=egl
```

如果 EGL 不可用，再改用：

```bash
export MUJOCO_GL=osmesa
```

## 3. 验证 CUDA / PyTorch / SmolVLA

当前已经匹配好的版本是：

- GPU：NVIDIA GeForce RTX 4090
- Driver：535.309.01
- PyTorch：`2.7.1+cu118`
- CUDA build：`11.8`
- LeRobot：`0.5.2`

运行验证：

```bash
python - <<'PY'
import torch
import lerobot
from lerobot.policies.smolvla.configuration_smolvla import SmolVLAConfig

print("torch:", torch.__version__)
print("torch cuda build:", torch.version.cuda)
print("cuda available:", torch.cuda.is_available())
if torch.cuda.is_available():
    print("gpu:", torch.cuda.get_device_name(0))
print("lerobot:", getattr(lerobot, "__version__", "unknown"))
print("smolvla:", SmolVLAConfig.__name__)
PY
```

期望输出里应包含：

```text
torch: 2.7.1+cu118
torch cuda build: 11.8
cuda available: True
gpu: NVIDIA GeForce RTX 4090
lerobot: 0.5.2
smolvla: SmolVLAConfig
```

如果 PyTorch 被误装坏，可以用国内源重装：

```bash
bash scripts/install_torch_cu118_cn.sh
```

## 4. 启动 MuJoCo 抓取方块 Demo

如果你有本地桌面、VNC 或 X11 转发，可以直接开 MuJoCo 实时窗口：

```bash
unset MUJOCO_GL
cube-grasp-viewer --loop
```

只打开场景、不播放脚本专家：

```bash
unset MUJOCO_GL
cube-grasp-viewer --manual
```

查看固定相机渲染出来的图像：

```bash
MUJOCO_GL=egl python examples/03_get_camera_image.py
```

当前 XML 里有两个相机：

```text
front
top_oblique
```

图片会保存到：

```text
outputs/camera_images/
```

实时查看固定相机视频画面，不保存图片：

```bash
unset MUJOCO_GL
python examples/04_live_camera_view.py --camera front
```

顶视角相机：

```bash
unset MUJOCO_GL
python examples/04_live_camera_view.py --camera top_oblique
```

窗口快捷键：

```text
关闭窗口 退出
```

如果你看到 `X11: Failed to open display`，说明当前终端没有图形窗口环境。需要在本地桌面、VNC、NoMachine 或 X11 转发里运行。

如果你看到 OpenCV 的 `cvNamedWindow` / `The function is not implemented`，说明当前 OpenCV 是 headless 版本。直接使用默认 MuJoCo backend，不要加 `--backend opencv`。

如果你是在纯 SSH 服务器里，没有桌面/VNC/NoMachine，但又想实时看视频画面，可以启动浏览器视频流：

```bash
MUJOCO_GL=egl python examples/05_camera_stream_server.py --camera front --host 0.0.0.0 --port 8000
```

然后在浏览器打开：

```text
http://服务器IP:8000
```

本机运行时打开：

```text
http://127.0.0.1:8000
```

键盘控制机械臂：

```bash
unset MUJOCO_GL
python examples/06_keyboard_control_arm.py --camera front
```

按键映射：

```text
A / D : panda_joint1 -/+
W / S : panda_joint2 +/-
Q / E : panda_joint3 -/+
R / F : panda_joint4 +/-
T / G : panda_joint5 +/-
Y / H : panda_joint6 +/-
Z / X : gripper open/close
B     : 回到 home 姿态
N     : 重置方块和机械臂
P     : 在终端打印当前目标值
```

这个例子控制的是 7 维动作空间里的真实关节目标：

```text
[panda_joint1, panda_joint2, panda_joint3, panda_joint4, panda_joint5, panda_joint6, gripper_closed]
```

更容易手动抓取的方式是控制夹爪末端位置：

```bash
unset MUJOCO_GL
python examples/07_keyboard_control_ee.py --camera front
```

基础按键：

```text
W / S : x forward/back
A / D : y left/right
R / F : raise/lower gripper
Z / X : open/close gripper fully
B     : 回到 home 目标点
N     : 重置方块和机械臂
P     : 打印当前末端目标和抓取状态
```

辅助抓取快捷键：

```text
1 : 移动到方块上方
2 : 下探到方块
3 : 闭合夹爪
4 : 抬起方块
5 : 移动到托盘上方
6 : 下降到托盘
7 : 张开夹爪
```

这个例子的控制链路是：

```text
键盘 -> 末端目标 [x, y, z_down, gripper_closed]
     -> 简化 IK
     -> 7 维关节动作
     -> env.step(action)
```

场景里已经加入了抓取参照物：

```text
红色轴：桌面 X 方向
绿色轴：桌面 Y 方向
蓝色轴：桌面 Z 方向
绿色竖杆/横杠：cube 抬起高度参考线
青色十字准星：夹爪末端对准点
橙色/紫色 cube：随机摆放的干扰方块，当前任务仍然抓蓝色 cube
```

如果 cube 被夹住并抬过绿色横杠附近，基本可以判断抓取成功。程序内部的成功判断还可以按 `P` 查看：

```text
grasped=True
can_grasp=True
```

如果是在无显示器服务器上，则继续使用离屏渲染并输出视频：

回放刚采集的 10 条 raw npz 动作轨迹：

```bash
unset MUJOCO_GL
python examples/08_replay_recorded_actions.py
```

只回放其中一条：

```bash
python examples/08_replay_recorded_actions.py --episode 3 --camera top_oblique
```

运行一条脚本专家轨迹，并输出视频：

```bash
MUJOCO_GL=egl cube-grasp-demo \
  --episodes 1 \
  --steps 280 \
  --width 640 \
  --height 480 \
  --out outputs/cube_grasp_demo.mp4
```

成功时会看到类似：

```text
wrote outputs/cube_grasp_demo.mp4
scripted grasp/lift success episodes: 1/1
```

顶视角视频：

```bash
MUJOCO_GL=egl cube-grasp-demo \
  --camera top_oblique \
  --episodes 1 \
  --steps 280 \
  --out outputs/cube_grasp_top.mp4
```

## 5. 采集 LeRobot 数据集

当前动作空间已经改成 7 维：

```text
panda_joint1
panda_joint2
panda_joint3
panda_joint4
panda_joint5
panda_joint6
gripper_closed
```

也就是 6 个 Panda 风格机械臂关节目标，加 1 个夹爪闭合量。旧的 4 维笛卡尔动作数据集不要和新版混用，需要重新采集。

数据采集时，环境来自 `env.py`，专家动作来自 `scripted_grasp.py`：

```python
from lerobot_smolvla_mujoco_demo import CubeGraspEnv, scripted_grasp_action

env = CubeGraspEnv()
obs, info = env.reset()
action = scripted_grasp_action(env, step=0)
obs, reward, terminated, truncated, info = env.step(action)
```

先做小规模测试：

```bash
MUJOCO_GL=egl cube-grasp-record \
  --episodes 5 \
  --steps 280 \
  --repo-id local/panda_6dof_7ctrl_test \
  --success-only
```

正式采集建议 50 到 200 条：

```bash
MUJOCO_GL=egl cube-grasp-record \
  --episodes 100 \
  --steps 280 \
  --repo-id local/panda_6dof_7ctrl \
  --success-only
```

训练 SmolVLA 时，不建议把失败的手动抓取轨迹混进去。推荐优先用脚本专家采集成功数据：

```text
脚本专家数据 80% 到 100%
键盘末端控制成功数据 0% 到 20%
失败轨迹先不要加入训练集
```

原因是 SmolVLA 做行为克隆时会学习你给它的动作。如果数据里大量包含“对不准、夹空、乱动”的轨迹，模型会把这些也学进去。

输出位置：

```text
outputs/cube_grasp_raw/
outputs/lerobot_datasets/local/panda_6dof_7ctrl/
```

## 6. 启动 SmolVLA 训练

先用刚采集的 10 条成功轨迹测试训练流程：

```bash
bash scripts/train_smolvla_10_success.sh
```

默认训练脚本：

```bash
DATASET_REPO_ID=local/panda_6dof_7ctrl \
DATASET_ROOT=outputs/lerobot_datasets \
OUTPUT_DIR=outputs/smolvla_panda_6dof_7ctrl \
STEPS=5000 \
BATCH_SIZE=16 \
bash scripts/train_smolvla_cube.sh
```

如果显存不足，先降低 batch size：

```bash
BATCH_SIZE=4 STEPS=5000 bash scripts/train_smolvla_cube.sh
```

训练输出目录：

```text
outputs/smolvla_panda_6dof_7ctrl/
```

## 7. 常见问题

### `cuda_available` 在工具里是 False

Codex 工具默认沙箱里可能看不到 `/dev/nvidia*`，因此会显示 `cuda_available=False`。在你自己的真实终端中激活 conda 环境后运行验证脚本，已确认可以看到 RTX 4090。

### `nvidia-smi` 报错

在普通沙箱命令里可能无法访问 GPU。请在系统终端直接运行：

```bash
nvidia-smi
```

### MuJoCo 无法渲染

先设置：

```bash
export MUJOCO_GL=egl
```

如果还是失败：

```bash
export MUJOCO_GL=osmesa
```

### 重新安装 CUDA 版 Torch

使用已写好的国内源脚本：

```bash
cd /home/mkls/xiao_run/lerobot_smolvla_mujoco_demo
conda activate /home/mkls/xiao_run/.conda-lerobot-smolvla
bash scripts/install_torch_cu118_cn.sh
```

## 8. 一键启动顺序

最常用的完整流程如下：

```bash
source /home/mkls/anaconda3/etc/profile.d/conda.sh
conda activate /home/mkls/xiao_run/.conda-lerobot-smolvla
cd /home/mkls/xiao_run/lerobot_smolvla_mujoco_demo
export MUJOCO_GL=egl

python - <<'PY'
import torch
print(torch.__version__, torch.version.cuda, torch.cuda.is_available())
if torch.cuda.is_available():
    print(torch.cuda.get_device_name(0))
PY

cube-grasp-demo --episodes 1 --out outputs/cube_grasp_demo.mp4
cube-grasp-record --episodes 100 --repo-id local/panda_6dof_7ctrl

DATASET_REPO_ID=local/panda_6dof_7ctrl \
DATASET_ROOT=outputs/lerobot_datasets \
OUTPUT_DIR=outputs/smolvla_panda_6dof_7ctrl \
STEPS=5000 \
BATCH_SIZE=16 \
bash scripts/train_smolvla_cube.sh
```
